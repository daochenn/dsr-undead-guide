{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dev-watcher",
  "description": "dev-only permissions for the DS3 Save Watcher (mtime polling + capture dirs); enabled via tauri.dev.conf.json, excluded from release",
  "windows": [
    "main"
  ],
  "permissions": [
    "fs:allow-stat",
    "fs:allow-mkdir",
    "fs:allow-read-dir",
    "fs:scope-appdata-recursive",
    "fs:scope-home-recursive"
  ]
}
